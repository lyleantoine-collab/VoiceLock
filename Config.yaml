# config.yaml — Secure VoiceLock v1.1
backend: "mfcc"
storage: "json"
threshold: 0.8
duration: 3.0
sample_rate: 16000
require_liveness: true
liveness_phrase: "random"
default_user: "lyle"
anti_replay: true          # NEW: Block replay attacks
max_replay_age: 60         # Seconds
