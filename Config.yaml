# config.yaml — Torch-Free Super Saiyan
backend: "mfcc"              # "mfcc" only (no torch)
storage: "json"              # "json" or "sqlite"
threshold: 0.8
duration: 3.0
sample_rate: 16000
require_liveness: true
liveness_phrase: "random"
default_user: "lyle"
max_users: 1000
